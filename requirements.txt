# BusinessVault — Python dependencies
# Install with:  pip install -r requirements.txt
# Pinned to the versions currently running on 2026-06-23.

# --- Web framework / server ---
fastapi==0.136.3
uvicorn[standard]==0.49.0      # [standard] provides watchfiles for --reload
python-multipart==0.0.32       # required by FastAPI for form & file uploads

# --- Documents & PDF ---
python-docx==1.2.0             # staff contract / offer-letter generation
reportlab==4.5.1               # rota PDF export, manager's reports
pdfplumber==0.11.10           # invoice PDF text extraction (/invoices/extract-pdf)
Pillow>=12.2.0                 # invoice PDF thumbnails (pdfplumber 0.11.10 needs >=12.2)

# --- Data / utilities ---
haversine==2.9.0               # GPS geofencing for mobile clock-in
openpyxl==3.1.5                # used by the data-import helper scripts (import_*.py)
